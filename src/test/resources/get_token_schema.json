{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "A body with token that must be returned for Auth request",
  "type": "object",
  "properties": {
    "token": {
      "type": "string"
    }
  },
  "required": ["token"]
}